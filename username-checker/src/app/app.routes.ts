import { Routes } from '@angular/router';
import { UsernameCheckerComponent } from './components/username-checker/username-checker.component';

export const routes: Routes = [
  { path: '', component: UsernameCheckerComponent }
];
